// Root layout - minimal wrapper for next-intl App Router setup.
// The html/body tags and all providers are in [locale]/layout.tsx.

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
